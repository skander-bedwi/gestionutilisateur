{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}
 

{% block body %}
    <p style="color: #7b7b9c;font-size:30px;text-align: center;margin-top: revert;margin-bottom: revert;">Utilisateur</p>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ utilisateur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ utilisateur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ utilisateur.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('utilisateur_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('utilisateur_edit', {'id': utilisateur.id}) }}" class="btn btn-success">edit</a>

   <a class="btn btn-danger" style="height:40px"> {{ include('utilisateur/_delete_form.html.twig') }}</a>
{% endblock %}
